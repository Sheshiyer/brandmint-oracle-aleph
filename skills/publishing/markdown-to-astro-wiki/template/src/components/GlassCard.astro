---
interface Props {
  href?: string;
  interactive?: boolean;
  tone?: 'default' | 'warm' | 'signal' | 'accent';
  class?: string;
}

const {
  href,
  interactive = false,
  tone = 'default',
  class: className = '',
} = Astro.props;

const Tag = href ? 'a' : 'div';
---

<!--
  Refero-design: GlassCard is a legacy wrapper — prefer sections + borders + media blocks per anti-slop.
  Use only for interactive containers (clickable items, forms, expandable panels) where the border/shadow
  carries meaningful interaction affordance.
-->
<Tag
  href={href}
  class:list={[
    'glass-card',
    `glass-card--${tone}`,
    { 'glass-card--interactive': interactive || Boolean(href) },
    className,
  ]}
>
  <slot />
</Tag>
