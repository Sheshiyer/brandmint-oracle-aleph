{
  "mcpServers": {
    "brandmint": {
      "command": "bm",
      "args": ["mcp-serve"],
      "env": {
        "FAL_KEY": "${FAL_KEY}"
      },
      "description": "Brandmint brand identity orchestration - 44 skills for text strategy, visual assets, and campaign copy"
    }
  }
}
