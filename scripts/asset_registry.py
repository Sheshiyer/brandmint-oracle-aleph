"""
asset_registry.py — Domain-aware asset selection engine (Phase 3).

Future: Replace fixed 19-asset template with 50+ tagged asset types.
Brand configs declare domain_tags, registry auto-selects matching assets.

Example:
    brand:
      domain_tags: ["marketplace", "app", "travel"]

    Asset types like App Icon, Instagram Story, OG Image would auto-select
    based on domain tags. Legacy prompts section remains backward-compatible.
"""
# TODO: Phase 3 implementation
